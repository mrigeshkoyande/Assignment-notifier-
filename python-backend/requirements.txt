# Assignment Notifier — Python Backend Dependencies
# Last updated: February 28, 2026
# Python 3.8+ required
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────────────────────
flask              # Lightweight WSGI web framework
flask-cors         # Cross-Origin Resource Sharing (CORS) headers for Flask

# ── Computer Vision ────────────────────────────────────────────────────────────
opencv-python      # OpenCV: camera feed, image processing, and frame capture

# ── Optional: Enhanced image handling ─────────────────────────────────────────
# Pillow           # Uncomment if additional image processing is needed
